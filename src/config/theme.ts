import {Dimensions} from 'react-native';

export const {width: W, height: H} = Dimensions.get('screen');
export const ITEM_WIDTH = W * 0.7;
export const width = W;
export const SPACING = 12;
export const ICON_SIZE = 64;
